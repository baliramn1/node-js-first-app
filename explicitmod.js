const randomvar = "randomvar0 from explicitmod here"
const randomvar1 = "randomvar1 from explicitmod here"
const randomvar2 = "randomvar2 from explicitmod here"
export const randomvar3 = "randomvar2 from explicitmod here"

const generateRandom = () => {
    return `Random score is ${Math.round(Math.random() * 100)}%`
}


export default randomvar;
export  {randomvar1, randomvar2, generateRandom}
 

